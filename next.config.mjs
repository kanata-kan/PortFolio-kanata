/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'source.unsplash.com',
      'mobisoftinfotech.com',
      'imgbb.com',
      'i.ibb.co',
    ],
  },
};

export default nextConfig;
